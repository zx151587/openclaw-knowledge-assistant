# 📋 OpenClaw 知识收集系统 — 项目运行流程

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          用户输入层                                      │
│   飞书私聊/群聊  │  WebChat  │  CLI 终端  │  GitHub Web                  │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     A：OpenClaw 网关（路由层）                            │
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────────┐   │
│  │ 飞书渠道插件   │  │ Git 版本管理  │  │ 跨模块路径 & 环境变量         │   │
│  │ 私聊/群聊路由  │  │ main分支合并  │  │ 统一配置管理                 │   │
│  └──────┬───────┘  └──────┬───────┘  └──────────────┬───────────────┘   │
└─────────┼──────────────────┼─────────────────────────┼──────────────────┘
          │                  │                         │
          ▼                  ▼                         ▼
```

## 二、数据流（采集 → 存储 → 检索 → 推送）

```
  [互联网网页]                    [用户问题]
       │                              │
       ▼                              │
┌──────────────┐                      │
│  B: 采集层    │                      │
│              │                      │
│ collect.mjs  │                      │
│  ├─ URL校验   │                      │
│  ├─ 网页抓取  │                      │
│  ├─ DeepSeek  │                      │
│  │  摘要标签   │                      │
│  └─ 入库      │                      │
│              │                      │
│ knowledge    │                      │
│ .json        │                      │
└──────┬───────┘                      │
       │                              │
       ▼                              ▼
┌──────────────────────────────────────────────┐
│              C：知识库与 RAG 层                │
│                                              │
│  ┌────────────────┐  ┌──────────────────┐    │
│  │ ChromaDB 向量库 │  │  query-kb.mjs    │    │
│  │                │  │                  │    │
│  │ 文档解析        │  │ 语义检索         │    │
│  │ (PDF/Word)     │  │ RAG 问答生成     │    │
│  │ 文本分块        │  │                  │    │
│  │ 向量嵌入        │  │ 返回答案给用户    │    │
│  └────────────────┘  └──────────────────┘    │
└──────────────────────┬───────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────┐
│                D：自动化与集成层                         │
│                                                        │
│  ┌────────────────┐  ┌────────────────────────────┐   │
│  │ Cron 定时任务   │  │  飞书消息推送               │   │
│  │                │  │                            │   │
│  │ 每天 9:00 触发  │  │  推送每日简报到飞书群聊      │   │
│  │ openclaw cron  │  │  内容: 新增知识数量/摘要/标签 │   │
│  │ run <job-id>   │  │  异常: "昨日暂无新增知识"     │   │
│  └────────────────┘  └────────────────────────────┘   │
└──────────────────────────────────────────────────────┘
```

## 三、各组件详细说明

### 🔵 A 组 — 组长/全栈（你）

| 组件 | 作用 | 关键技术 |
|------|------|----------|
| **OpenClaw 网关** | 系统核心路由，连接所有通道 | OpenClaw Gateway |
| **飞书渠道插件** | 对接飞书私聊/群聊，接收指令 | feishu-channel Skill |
| **Git 管理** | 仓库管理、分支合并、版本控制 | Git + GitHub |
| **环境配置** | 跨模块路径、环境变量、API Key | openclaw.json |

**负责：** 搭架子、配通道、审合并、保集成。

---

### 🟢 B 组 — Skill 开发（已交付 ✅）

| 组件 | 文件 | 作用 |
|------|------|------|
| **SKILL 定义** | `SKILL.md` | 技能说明书，定义触发词和用法 |
| **URL 安全校验** | `scripts/collect.mjs` | 拒绝内网/非标准协议，防止 SSRF 攻击 |
| **网页抓取** | `scripts/collect.mjs` | 抓取网页正文，自动处理 GBK/UTF-8 编码 |
| **AI 摘要标签** | `scripts/collect.mjs` | 调用 DeepSeek 生成结构化摘要和标签 |
| **知识入库** | `scripts/store.mjs` | JSON 文件存储，CRUD 操作 |
| **知识检索** | `scripts/query.mjs` | 列表/标签/关键词/ID 多维查询 |
| **每日简报** | `scripts/digest.mjs` | 统计昨日新增，支持 JSON 输出对接 |
| **可视化看板** | `dashboard.html` | SVG 图表、标签云、搜索、趋势图 |

**负责：** 网页知识采集→入库的完整 pipeline。

---

### 🟡 C 组 — 知识库与 RAG（待开发）

| 组件 | 作用 |
|------|------|
| **ChromaDB 搭建** | 向量数据库，存储文档嵌入 |
| **文档解析** | PDF / Word 文档解析，提取正文 |
| **文本分块** | 将长文档切分为语义块 |
| **向量嵌入** | 调用 Embedding 模型生成向量 |
| **query-kb.mjs** | 语义检索入口 |
| **RAG 问答** | 检索 + LLM 生成回答 |

**数据来源：** B 组的 `knowledge.json` + PDF/Word 文档
**对外接口：** `query-kb.mjs --query "问题"` → 返回答案

---

### 🟠 D 组 — 自动化与集成（待开发）

| 组件 | 作用 |
|------|------|
| **Cron 任务创建** | `openclaw cron add` 创建定时任务 |
| **每日简报触发** | 每天早上 9:00 自动运行 `digest.mjs --daily` |
| **飞书消息推送** | 将简报通过飞书渠道发送到群聊 |
| **数据流协调** | 确保采集→入库→检索→推送链路通畅 |

**对接 B 组：** `digest.mjs --daily --json` 直接输出可推送格式
**对接 A 组：** 通过 OpenClaw Cron 系统管理

---

### 🔴 E 组 — 测试与文档（待开发）

| 组件 | 作用 |
|------|------|
| **全流程测试** | 端到端测试：采集→入库→检索→简报→推送 |
| **演示数据** | 准备 5-10 条示例知识数据 |
| **部署文档** | 环境搭建、依赖安装、配置说明 |
| **设计说明书** | 架构设计、接口文档、数据格式 |
| **答辩 PPT** | 项目演示幻灯片 |

---

## 四、验收标准对照

| # | 验收项 | 标准 | 负责 | 状态 |
|---|--------|------|------|------|
| 1 | 任务创建 | `openclaw cron list` 可看到任务 | D | ⏳ |
| 2 | 手动运行 | `openclaw cron run <job-id>` 能触发 | D | ⏳ |
| 3 | 自动推送 | 到设定时间能自动发送飞书消息 | A+D | ⏳ |
| 4 | 简报内容 | 包含新增知识数量、摘要、标签、复习问题 | B+D | ✅ B已完成 |
| 5 | 异常处理 | 昨日无新增时返回"昨日暂无新增知识" | B | ✅ |

## 五、第一次运行完整流程

```bash
# 1. 克隆项目
git clone https://github.com/zx151587/openclaw-knowledge-assistant.git
cd openclaw-knowledge-assistant

# 2. B 组：安装依赖 + 收集知识
npm install
node scripts/collect.mjs --url "https://example.com/article"

# 3. 启动看板（本地预览）
npx http-server . -p 8080
# 浏览器打开 http://localhost:8080/dashboard.html

# 4. C 组：迁移数据到 ChromaDB
# (读取 knowledge.json → 向量化 → 存入 ChromaDB)

# 5. D 组：创建 Cron 定时任务
openclaw cron add --name daily-digest \
  --schedule "0 9 * * *" \
  --command "node scripts/digest.mjs --daily --json"

# 6. A 组：开启 GitHub Pages，所有人可用在线看板
# Settings → Pages → Source: main /docs
```

## 六、关键对接点

```
B → C:  data/knowledge.json  (结构化数据)
B → D:  digest.mjs --json    (简报格式)
A → B:  环境变量/路径集成     (配置管理)
A → C:  OpenClaw 路由配置     (通道集成)
A → D:  Cron 任务注册         (定时调度)
E → 全:  测试用例 + 文档      (质量保障)
```
